# --- DEPENDENCIES ---
/node_modules
/.pnp
.pnp.js

# --- TESTING ---
/coverage

# --- BUILD ---
/build

# --- ENV VARIABLES ---
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# --- SYSTEM FILES ---
.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- FIREBASE KEYS (секретні!) ---
src/firebaseSDK/
*firebase-adminsdk*.json

# --- NODE.JS MIGRATION SCRIPTS (не для production build) ---
# Самі скрипти в git — ОК (не містять ключів).
# Ключі лежать в src/firebaseSDK/ — вже ігноруються вище.
# src/scripts/ — стара папка, можна видалити вручну після верифікації scripts/

# --- CUSTOM SCRIPTS / UTILS (не публічне API) ---
updateReactions.js
migrateTracks.js
cors.json
knitly@0.1.0

# --- PROJECT MEMORY (ніколи не в git) ---
KNITLY_CONTEXT.md
LAUNCH_NOTES.md
